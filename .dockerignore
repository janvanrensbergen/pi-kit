# VCS / IDE / tooling
.git
.gitignore
.idea
.DS_Store

# local project-level pi config (never shipped inside the baked kit image)
.pi

# build artifacts
node_modules
npm-debug.log*

# files not needed inside the baked kit image
Dockerfile
.dockerignore
install.sh
spec.yaml
.github